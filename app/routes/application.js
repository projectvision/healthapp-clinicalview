import Ember from 'ember';
import ApplicationRouteMixin from 'simple-auth/mixins/application-route-mixin';

//console.log(Ember);
//console.log(ApplicationRouteMixin);

export default Ember.Route.extend(ApplicationRouteMixin);
